# Environment variables — copy to .env and fill in values
# Not required for basic operation; add secrets here for production

# Optional: set log level
LOG_LEVEL=info

# Optional: max file upload size (bytes)
MAX_UPLOAD_MB=50
